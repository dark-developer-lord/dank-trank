# TEMPLATE — do NOT commit real values to source control.
# Fill in base64-encoded values, then apply with:
#   kubectl apply -f k8s/secret.yaml -n {{namespace}}
apiVersion: v1
kind: Secret
metadata:
  name: {{projectName}}-secret
  namespace: {{namespace}}
  labels:
    app.kubernetes.io/name: {{projectName}}
    app.kubernetes.io/managed-by: dank-trank
type: Opaque
data:
  # base64 encode values: echo -n 'value' | base64
  SECRET_KEY: <base64-encoded-value>
  DATABASE_URL: <base64-encoded-value>
  # Add more secrets below
